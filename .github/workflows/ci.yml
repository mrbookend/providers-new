name: ci

on:
  workflow_dispatch: {}   # manual only; no push/PR runs

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-guards:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "CI disabled for this repo."
