#!/usr/bin/env bash
set -euo pipefail
if [ "$(git rev-parse --abbrev-ref HEAD)" = "main" ]; then
  ~/bin/hcrcheck || {
    echo "WARNING: mismatch detected after commit on main."
    echo "Run: git pull --ff-only && ~/bin/hcrcheck"
  }
fi
